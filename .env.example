# Copy to .env (or export directly) for local runs.

ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-4-8

# Required unless running with --dry-run
REPORT_BUCKET=

# Optional tuning
WEB_SEARCH_MAX_USES=15
LOOKAHEAD_DAYS=14
LOOKBACK_DAYS=7
